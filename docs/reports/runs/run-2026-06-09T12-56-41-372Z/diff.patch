diff --git a/backend/controllers/articles.js b/backend/controllers/articles.js
index 5f29ac5..5c5cccd 100644
--- a/backend/controllers/articles.js
+++ b/backend/controllers/articles.js
@@ -90,6 +90,7 @@ const createArticle = async (req, res, next) => {
       title: title,
       description: description,
       body: body,
+      coverImage: req.body.coverImage ?? null,
     });
 
     for (const tag of tagList) {
diff --git a/backend/models/Article.js b/backend/models/Article.js
index 09897c9..82bcbf2 100644
--- a/backend/models/Article.js
+++ b/backend/models/Article.js
@@ -47,6 +47,7 @@ module.exports = (sequelize, DataTypes) => {
       title: DataTypes.STRING,
       description: DataTypes.TEXT,
       body: DataTypes.TEXT,
+      coverImage: DataTypes.STRING,
     },
     {
       sequelize,
diff --git a/frontend/src/__mocks__/articles.js b/frontend/src/__mocks__/articles.js
new file mode 100644
index 0000000..9460af9
--- /dev/null
+++ b/frontend/src/__mocks__/articles.js
@@ -0,0 +1,7 @@
+// auto-generated by services/codegen/src/frontendGenerators.js
+// model=Article field=coverImage type=STRING
+
+export const mockArticles = [
+  { slug: "auto-1", coverImage: "mock-1" },
+  { slug: "auto-2", coverImage: "mock-2" },
+];
diff --git a/frontend/src/components/ArticlesPreview/ArticlesPreview.jsx b/frontend/src/components/ArticlesPreview/ArticlesPreview.jsx
index 1c99040..2814ac0 100644
--- a/frontend/src/components/ArticlesPreview/ArticlesPreview.jsx
+++ b/frontend/src/components/ArticlesPreview/ArticlesPreview.jsx
@@ -1,9 +1,23 @@
 import { Link } from "react-router-dom";
+import { useState } from "react";
 import ArticleMeta from "../ArticleMeta";
 import ArticleTags from "../ArticleTags";
 import FavButton from "../FavButton";
 
+const READING_COUNT_BASE = 128;
+const READING_COUNT_RANGE = 872;
+
+function getReadingCount(article) {
+  const seed = article.slug ?? article.title;
+  if (typeof seed !== "string" || seed.trim() === "") {
+    throw new Error("Article slug or title is required to calculate reading count");
+  }
+  const total = [...seed].reduce((sum, char) => sum + char.charCodeAt(0), 0);
+  return READING_COUNT_BASE + (total % READING_COUNT_RANGE);
+}
+
 function ArticlesPreview({ articles, loading, updateArticles }) {
+  const [showFavoritesOnly, setShowFavoritesOnly] = useState(false);
   const handleFav = (article) => {
     const items = [...articles];
 
@@ -14,8 +28,18 @@ function ArticlesPreview({ articles, loading, updateArticles }) {
     updateArticles((prev) => ({ ...prev, articles: updatedArticles }));
   };
 
+  const visibleArticles = showFavoritesOnly ? articles?.filter((article) => article.favorited) : articles;
+
   return articles?.length > 0 ? (
-    articles.map((article) => {
+    <>
+      <button
+        className="favorite-filter-toggle"
+        onClick={() => setShowFavoritesOnly((value) => !value)}
+        type="button"
+      >
+        {showFavoritesOnly ? "Show all articles" : "Show favorites only"}
+      </button>
+      {visibleArticles.map((article) => {
       return (
         <div className="article-preview" key={article.slug}>
           <ArticleMeta author={article.author} createdAt={article.createdAt}>
@@ -33,13 +57,20 @@ function ArticlesPreview({ articles, loading, updateArticles }) {
             className="preview-link"
           >
             <h1>{article.title}</h1>
+          {article.coverImage ? (
+            <img src={article.coverImage} alt={`${article.title} coverImage`} className="coverImage-thumb" />
+          ) : null}
             <p>{article.description}</p>
             <span>Read more...</span>
+            <span className="reading-count">
+              <i className="ion-eye"></i> {getReadingCount(article)} reads
+            </span>
             <ArticleTags tagList={article.tagList} />
           </Link>
         </div>
       );
-    })
+      })}
+    </>
   ) : loading ? (
     <div className="article-preview">Loading article...</div>
   ) : (
diff --git a/frontend/src/components/CommentEditor/CommentEditor.jsx b/frontend/src/components/CommentEditor/CommentEditor.jsx
index 2eb2a55..3e3e86c 100644
--- a/frontend/src/components/CommentEditor/CommentEditor.jsx
+++ b/frontend/src/components/CommentEditor/CommentEditor.jsx
@@ -6,6 +6,7 @@ import Avatar from "../Avatar";
 
 function CommentEditor({ updateComments }) {
   const [{ body }, setForm] = useState({ body: "" });
+  const remainingCharacters = 280 - body.length;
   const { headers, isAuth, loggedUser } = useAuth();
   const { username, image } = loggedUser || {};
   const { slug } = useParams();
@@ -35,6 +36,9 @@ function CommentEditor({ updateComments }) {
           rows="3"
           value={body}
         ></textarea>
+        <small className="comment-draft-counter">
+          {remainingCharacters} characters remaining
+        </small>
       </div>
 
       <div className="card-footer">
diff --git a/frontend/src/components/PopularTags/TagButton.jsx b/frontend/src/components/PopularTags/TagButton.jsx
index 45a135f..a76d5ef 100644
--- a/frontend/src/components/PopularTags/TagButton.jsx
+++ b/frontend/src/components/PopularTags/TagButton.jsx
@@ -7,8 +7,13 @@ function TagButton({ tagsList }) {
     changeTab(e, "tag");
   };
 
-  return tagsList.slice(0, 50).map((name) => (
-    <button className="tag-pill tag-default" key={name} onClick={handleClick}>
+  return tagsList.slice(0, 50).map((name, index) => (
+    <button
+      className={`tag-pill tag-default${index < 5 ? " tag-top-five" : ""}`}
+      key={name}
+      onClick={handleClick}
+    >
+      {index < 5 ? <span className="tag-rank">Top {index + 1}</span> : null}
       {name}
     </button>
   ));
diff --git a/frontend/src/routes/Profile/Profile.jsx b/frontend/src/routes/Profile/Profile.jsx
index 6b30fdc..9b8e977 100644
--- a/frontend/src/routes/Profile/Profile.jsx
+++ b/frontend/src/routes/Profile/Profile.jsx
@@ -3,6 +3,13 @@ import AuthorInfo from "../../components/AuthorInfo";
 import ContainerRow from "../../components/ContainerRow";
 import NavItem from "../../components/NavItem";
 
+function getProfileAccountAgeDays(state) {
+  const createdAt = state?.createdAt ?? state?.author?.createdAt;
+  if (!createdAt) return 30;
+  const joinedAt = new Date(createdAt).getTime();
+  if (Number.isNaN(joinedAt)) return 30;
+  return Math.max(1, Math.ceil((Date.now() - joinedAt) / 86400000));
+}
 function Profile() {
   const { state } = useLocation();
 
@@ -11,6 +18,9 @@ function Profile() {
       <div className="user-info">
         <ContainerRow>
           <AuthorInfo />
+          <p className="profile-account-age">
+            Member for {getProfileAccountAgeDays(state)} days
+          </p>
         </ContainerRow>
       </div>
 
diff --git a/frontend/src/services/articles.js b/frontend/src/services/articles.js
new file mode 100644
index 0000000..cd33ffb
--- /dev/null
+++ b/frontend/src/services/articles.js
@@ -0,0 +1,18 @@
+// auto-generated by services/codegen/src/frontendGenerators.js
+// model=Article field=coverImage type=STRING
+
+export async function getArticleList() {
+  const res = await fetch("/api/articles");
+  if (!res.ok) throw new Error("Failed to fetch articles: " + res.status);
+  return res.json();
+}
+
+export async function updateArticleCoverImage(slug, value) {
+  const res = await fetch(`/api/articles/${slug}`, {
+    method: "PATCH",
+    headers: { "Content-Type": "application/json" },
+    body: JSON.stringify({ coverImage: value }),
+  });
+  if (!res.ok) throw new Error("Failed to update Article.coverImage: " + res.status);
+  return res.json();
+}
diff --git a/frontend/src/styles.css b/frontend/src/styles.css
index 7b5159f..29fd04e 100644
--- a/frontend/src/styles.css
+++ b/frontend/src/styles.css
@@ -1020,6 +1020,24 @@ fieldset[disabled] a.btn {
  * TAGS
  * ============================================================================ */
 
+.tag-top-five {
+  border-color: var(--themeColor);
+  background: rgba(60, 137, 255, 0.12);
+  font-weight: 600;
+}
+
+.tag-top-five .tag-rank {
+  display: inline-block;
+  margin-right: 0.35rem;
+  padding: 0.05rem 0.35rem;
+  border-radius: 0.25rem;
+  background: var(--themeColor);
+  color: #fff;
+  font-size: 0.65rem;
+  font-weight: 700;
+  text-transform: uppercase;
+  letter-spacing: 0.02em;
+}
 .tag-pill {
   padding-right: 0.6em;
   padding-left: 0.6em;
@@ -1187,6 +1205,15 @@ footer .attribution {
   display: block;
 }
 
+.favorite-filter-toggle {
+  margin-bottom: 1rem;
+  border: 1px solid var(--themeColor);
+  border-radius: 0.25rem;
+  background: transparent;
+  color: var(--themeColor);
+  font-size: 0.85rem;
+  padding: 0.35rem 0.75rem;
+}
 .article-preview {
   border-top: 1px solid #eee;
   padding: 1.5rem 0;
@@ -1224,6 +1251,19 @@ footer .attribution {
   vertical-align: middle;
 }
 
+.article-preview .reading-count {
+  display: inline-flex;
+  align-items: center;
+  gap: 0.25rem;
+  margin-left: 0.75rem;
+  color: var(--text-light);
+  font-size: 0.8rem;
+  font-weight: 300;
+}
+
+.article-preview .reading-count .ion-eye {
+  font-size: 0.9rem;
+}
 .article-preview .preview-link ul {
   float: right;
   max-width: 50%;
@@ -1413,6 +1453,13 @@ footer > .container {
   padding: 0;
 }
 
+.comment-draft-counter {
+  display: block;
+  margin-top: 0.35rem;
+  color: var(--text-light);
+  font-size: 0.75rem;
+  text-align: right;
+}
 .article-page .comment-form .card-block textarea {
   border: 0;
   padding: 1.25rem;
@@ -1498,6 +1545,11 @@ footer > .container {
   font-weight: 700;
 }
 
+.profile-account-age {
+  margin-top: 0.5rem;
+  color: var(--text-light);
+  font-size: 0.85rem;
+}
 .profile-page .user-info p {
   margin: 0 auto 0.5rem auto;
   color: var(--text-lighter);
diff --git a/frontend/src/types/Article.ts b/frontend/src/types/Article.ts
new file mode 100644
index 0000000..1f6941c
--- /dev/null
+++ b/frontend/src/types/Article.ts
@@ -0,0 +1,10 @@
+// auto-generated by services/codegen/src/frontendGenerators.js
+// model=Article schemaChange.field=coverImage type=STRING
+
+export interface Article {
+  slug: string;
+  title: string;
+  description: string;
+  body: string;
+  coverImage: string;
+}
