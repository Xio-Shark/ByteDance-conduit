# 当前进度汇报：Conduit 超级个体端到端交付系统

更新时间：2026-05-22（AI evidence fail-fast 复核；Web controller / RunResult 面板、API run routes、runArchive、runEvidenceGuards、deliveryPipeline 控制流与 aiCallRecords 拆分；archive dry-run 已接入；与 `docs/10-progress.md` 同步；P1-5 豆包已决策跳过；根目录临时 CSV 已清理）

## 总体结论

当前项目处于 **「§2.1 MVP 与 §2.2 六项代码/run 证据已齐；§8.2 对外交付与答辩录屏仍待人工」** 的状态。核心任务已从「开发功能」转向 **「S7 答辩录屏 + S6/S8 对外链接闭合」**。

**一句话**：代码与 run 证据基本到位；距离 PDF「课题完成 / 可提交答辩」还差 **§8.2 对外交付四件套**（演示视频、公开 AI 系统主仓并内含 `sandbox-repo/`、团队/Demo 链接、六项统一录屏），以及若干加分项深度（跨栈全自动同步等）。

- **文档仓库** `/Users/xioshark/Desktop/bytedance`：只承担 `v1.0.0` 设计基线（PRD、架构、验收、追踪）；不含业务源码与运行证据。
- **实现仓库** `/Users/xioshark/Desktop/bytedance/bytedance-implementation`：三层应用、编排、Agent、**4 个 Skill**、sandbox、断点重放、跨 run 可观测、run 证据与提交材料均已落地；常规模式自动跑通，`BLOCK_ON_CONFIRM=1` 可演示真阻塞 confirm；代码级 P0 在显式 `AI_MODE=rules` 下闭环。
- **执行清单**：[`docs/06-plan.md#冲刺关键路径`](./docs/06-plan.md#冲刺关键路径进度-ssot)；根目录不再保留 CSV 台账，进度事实同步到 [`docs/10-progress.md`](./docs/10-progress.md) 与本摘要。

规则模式已证明代码级 P0 胶水链路（澄清 → 历史召回 → 方案 → Conduit 写入 → lint/单测 → PR 草稿）。**H1–H16 代码冲刺**已闭合；**课题完成仍缺**：§2.2 六项答辩级视频演示、公开 AI 系统主仓（仓内包含 `sandbox-repo/`）、Demo URL、团队姓名。真实 draft PR / 上游 Conduit PR 属于 H17 可选远端能力，当前暂缓，不阻塞 §8.2 最小提交。

**LLM 口径（团队共识）**：**不强制豆包**；§2.2 #6 以 `run-2026-05-21T05-58-01-181Z`（**模糊输入 + `clarifications[]` + 非零 tokens**，`mimo-v2.5`）为验收证据；P1-5「补豆包 clarify」**已决策跳过**（见 `ai-usage.md`）。早期 `aiMode: doubao` 清晰 L1 原句 run **不计入 §2.2 #6**。

本次复核在实现仓执行 `npm run verify`：**108 项 Node/API/Web/scripts 测试（107 pass / 1 skip 集成）、sandbox lint、Conduit Vitest（12 测试）、Web 生产构建全部通过**。`npm run archive:dry-run` 也已通过（347 files，7 条关键 run；`manifestHash=db973390d2344d49809a9b24d28a2437d9cf37341a6b28340d70e184b7b82751`，`contentHash=11a9f990204efe981035a12890232ca12045aaf9c32f82a0c672280525dff1e1`），证明本地候选发布包包含关键源码、submission、`sandbox-repo/`、脚本测试与 §2.2 run 证据，并排除 `.env`、`node_modules`、Web build 产物和测试结果目录；它不替代公开仓 URL、Demo URL、视频 URL、团队信息或最终提交。

---

## 对照源题 PDF 完成度

对照 [`实现一个可以端到端交付全栈项目的“超级个体”.pdf`](./实现一个可以端到端交付全栈项目的“超级个体”.pdf)（忽略 §7.1 凭据发放；§7.2 与 §8.2 仍有效）。逐项状态以本文件、[`docs/10-progress.md`](./docs/10-progress.md) 和实现仓 `docs/reports/submission/` 为准。

| 层级 | 状态 | 说明 |
|------|------|------|
| **§2.1 MVP（硬性）** | ✅ 已达标 | PM 输入 → 澄清 → 方案 → Conduit 实仓 → lint/单测 → PR 草稿 |
| **§2.2 六项（代码/证据）** | ✅ 已达标 | 六项均有 run/API/UI 证据；答辩录屏未闭合 |
| **§7.2 AI 留痕** | ✅ 已达标 | 调用日志、Prompt 版本、模型声明、合规说明 |
| **§8.2 提交材料** | 🟡 ~85% | 文档草稿齐；对外链接未填 |
| **课题完成（§2.1 + §2.2 + §8.2）** | ❌ 未闭合 | 阻塞在人工交付项 |

**验收追踪**：`docs/11-acceptance.yaml` **24/28 AC verified**；AC-F006-02（H17 真实远端 draft PR，可选）与 AC-F011-01（团队/Demo 链接）为 partial，AC-F011-02（视频）与 AC-F011-03（公开仓）为 manual_pending。

---

## 仓库边界

| 仓库 | 职责 | 状态 |
|------|------|------|
| `/Users/xioshark/Desktop/bytedance` | `v1.0.0` 设计基线、PRD、架构、验收 YAML、本汇报 | 仅维护文档；`AGENTS.md` 限定可改范围 |
| `…/bytedance-implementation` | Web、API、Orchestrator、Agents、Skills、sandbox、证据、提交草稿 | P0 + §2.2 + 实现增强已落地；`npm run verify` 通过 |
| `…/bytedance-implementation/sandbox-repo` | Conduit 真实代码改动目标 | 阅读量 / 草稿 / 字数 / Popular Tags 等多 Skill 改动 |

本文件是根目录**进度快照**，不替代唯一事实源。规格与验收以 `docs/01`–`12` 为准；**冲刺任务**以 [`docs/06-plan.md#冲刺关键路径`](./docs/06-plan.md#冲刺关键路径进度-ssot) 为准；运行证据以 `bytedance-implementation/docs/reports/**` 为准。

---

## 里程碑进度（对照 `docs/06-plan.md`）

| 里程碑 | 计划目标 | 当前状态 | 主要证据 |
|--------|----------|----------|----------|
| **M1** 课题理解与范围冻结 | Understanding / PRD / Spec / Design / Dev / Plan 对齐源题 | ✅ 已完成 | `docs/01`–`12` |
| **M2** 工程骨架与 Conduit 接入 | 三端可启动；sandbox 可读写 | ✅ 已完成 | `apps/web`、`apps/api`、`services/orchestrator`、`services/sandbox`、`.env.example` |
| **M3** P0 L1 链路跑通 | PM 输入 → 真实 diff | ✅ 已完成 | `run-2026-05-21T02-16-15-215Z` 等 |
| **M4** 验证与 PR 草稿闭环 | lint / 单测 + PR 描述 | ✅ 已完成 | `verification.json`；GitHub draft PR provider（无 token 时契约测试） |
| **M5** §2.2 编排与体验 | 断点重放、LLM 澄清、监控面板、历史入 plan | 🟡 代码已闭合 | resume-from-stage、ClarificationsPanel、Cross-run AI Usage；`BLOCK_ON_CONFIRM=1` 可演示真阻塞；**待 S7 录屏演示** |
| **M6** §2.2 能力与 Skill 扩展 | 第 2/3 Skill、L2 跨栈、答辩第 4 Skill | ✅ 已完成 | 4 Skill；L2 `05-52-12`；第 3 `05-52-18`；第 4 `popular-tags-top-five`（`06-24-47`） |
| **M7** 提交与答辩 | §8.2 材料、视频覆盖六项 | 🟡 草稿已生成 | `submission/*` 非空；视频/公开仓/团队 URL 待填 |

**官方时间节点**：实战期 2026-05-20 至 2026-06-10；成果提交截止 2026-06-10；答辩 2026-06-11 至 2026-06-19。距提交截止约 **20 天**。

---

## §2.2 评判亮点六项（对照 `docs/06-plan.md`）

| # | 亮点 | 代码 | 答辩/视频 | 说明 | 降级路径 |
|---|------|------|-----------|------|---------|
| 1 | 抽象到位 | ✅ | 🟡 | **4 Skill**；第 3/4 个仅新增 Skill 文件 | 答辩录「只加文件不改主干」 |
| 2 | 断点重放 | ✅ | 🟡 | `POST .../resume-from-stage` + Web resume；`checkpoints.json` | 录制 plan 后只重跑 edit→verify→pr |
| 3 | 跨栈一致性 | 🟡 | 🟡 | L2 `05-52-12`；`crossStackSync` 检查器（**非 PDF 理想态全自动**） | — |
| 4 | 可观测性 | ✅ | 🟡 | `05-58-01` 非零 metrics；单 run + **跨 run** AI Usage 面板 | **不可降级**；答辩须录面板 |
| 5 | 业务上下文反哺 | ✅ | 🟡 | `plan.history_references`；`05-51-56`；`sandbox_index` 入 plan | — |
| 6 | 澄清深度 | ✅ | 🟡 | 模糊 run `05-58-01` 含 `clarifications[]`；Web **ClarificationsPanel** | **不可降级** |

闭合依赖链见 [`docs/06-plan.md#冲刺关键路径`](./docs/06-plan.md#冲刺关键路径进度-ssot)。

---

## 实现仓增量（2026-05-21 闭合项）

| 类别 | 交付物 |
|------|--------|
| 答辩 Skill | `popular-tags-top-five`（`Popular Tags` 前 5 打标） |
| Web 体验 | `ClarificationsPanel` 多轮追问；`CrossRunAiUsagePanel` + `GET /api/ai-usage/summary`（只聚合 passed run；失败/不完整归档进 `skipped`） |
| 编排 | 常规模式自动跑通；`BLOCK_ON_CONFIRM=1` 真阻塞 + `POST /api/runs/:id/continue` 用于演示人工确认 |
| 工程 | `sandboxIndex` + `PLAN_MODE=llm`；`crossStackSync`；Skill **intent 加权匹配** |
| 质量 | Playwright E2E（`scripts/run-e2e.sh`）；`RUN_INTEGRATION=1` 集成测；LLM usage / `source_input` fail-fast 回归；`runIndex` + `prune-runs.mjs` |
| 文档 | README 端到端流程 + `architecture.md` 链接 |

**剩余实现增强项**：H17 真实 draft PR 暂缓。P1-5 豆包 clarify **不执行**（团队决策：§7.2 允许多模型，`mimo-v2.5` 已满足 #6）。

---

## 最新 Run 证据

### 代码级 P0（rules 主线）

| 项 | 当前值 |
|----|--------|
| **Run ID** | `run-2026-05-21T02-16-15-215Z` |
| **Skill** | `article-list-display-field` |
| **需求** | 文章列表加阅读量，前端假数据，不改后端 |

### §2.2 验收口径 LLM

| 项 | 当前值 |
|----|--------|
| **Run ID** | `run-2026-05-21T05-58-01-181Z` |
| **模糊输入** | 「文章列表想好看一点，加点数据，别动太多代码。」 |
| **可观测** | tokens_in 248 / tokens_out 1818 / latency 21854ms |

### 其它关键 run

| Run ID | 用途 |
|--------|------|
| `run-2026-05-21T05-51-56-519Z` | 相似需求召回入 plan |
| `run-2026-05-21T05-52-12-490Z` | L2 跨栈草稿 |
| `run-2026-05-21T05-52-18-277Z` | 第 3 Skill（详情字数） |
| `run-2026-05-21T06-24-47-248Z` | 第 4 Skill（Popular Tags 前 5） |

**累计 run 归档**：`docs/reports/runs/` 下已超过 **90** 个 run 目录（含成功、失败与本轮验证 run；具体数量会随验证 run 增长）。

---

## 验证与工程质量

| 环节 | 结果 |
|------|------|
| Node/API/Web/scripts 测试 | 通过（**108** 项；107 pass / 1 skip 集成） |
| `lint:sandbox` | 通过 |
| Conduit Vitest | 通过（12 测试） |
| Web 生产构建 | 通过 |
| Playwright E2E | `npm run test:e2e` 冒烟通过（需独立端口，见 `scripts/run-e2e.sh`） |

实现仓 `npm audit --audit-level=high`：**0 漏洞**。`sandbox-repo` 8 项上游漏洞见 `dependency-audit-decision.md`（waiver B，X1 已闭合）。

---

## 已完成能力（实现仓摘要）

- **4 个 Skill**：阅读量（L1）、草稿（L2 跨栈）、详情字数、Popular Tags 前 5。
- **断点重放**：`resume-from-stage` + checkpoints + Web 入口。
- **LLM 澄清 + UI**：模糊验收 run；Requirement 面板展示 `clarifications[]`。
- **可观测**：单 run AI Usage + 跨 run 汇总面板；跨 run totals 只统计 passed run，且要求 `run-summary.json.aiUsage` 与 `ai-calls.jsonl` 一致；失败/不完整归档明示为 `skipped`。
- **人工确认**：常规模式为事后留痕；Web 按钮显示为 Record review。演示真阻塞时启用 `BLOCK_ON_CONFIRM=1`，进入 `waiting_requirement_confirm` / `waiting_plan_confirm` 后通过 continue API 继续。
- **严格失败语义**：缺证据、低置信度/并列 Skill 匹配、缺真实 sandbox repo path、跨栈不一致等 fail-fast 或 degraded。
- **GitHub PR**：provider 已实现；H17 无 `GITHUB_TOKEN` 时仅本地 `pr-draft.md`。
- **提交材料**：checklist、architecture、ai-usage（含模型表/合规段）、video 脚本等已生成。

---

## 提交材料状态（§8.2）

| 材料 | 状态 |
|------|------|
| checklist / demo-script / architecture / ai-usage / engineering-notes | ✅ 已生成 |
| team-info / defense-prep / video-recording-guide / public-repo-guide | 草稿存在；**姓名、URL、视频、公开仓仍为 `pending_human`** |
| security-check-report | 🟡 S9 部分完成；本地扫描与 archive dry-run 已通过，发布当天重跑脚本、Git 历史检查与远端 secret scanning 未闭合 |
| dependency-audit-decision | ✅ X1 |
| **演示视频 / 公开 AI 主仓（内含 `sandbox-repo/`）/ Demo URL** | ❌ 待人工 |

实现仓 API 已按内容识别 submission readiness：`team-info.md` / `checklist.md` 仍含待填外链、待录制/待发布状态或未勾选最终提交项时显示为 `pending_human`；`scripts/pre-submission-check.sh` 当前会先运行 `npm run archive:dry-run`，再聚合报告 4 类外部门禁阻塞：这些 §8.2 人工项、发布清单路径未全部进入 Git 跟踪、`sandbox-repo/` / 关键源码 / run / submission 等发布路径仍未准备成 tracked public repository、最终外部提交未完成；readiness 失败后不会继续跑 `npm run verify`，避免把本地门禁通过误报为最终可提交。archive dry-run 是本地候选发布校验，不是远端公开仓证据。

§8.2 文档草稿约 **85% 就绪**；**对外可访问链接与答辩视频为最大缺口**。

---

## 完成判定

### 代码级 P0 — ✅ 已完成

### 课题完成（§2.1 + §2.2 + §8.2）— ❌ 未闭合

| 检查项 | 状态 |
|--------|------|
| §2.2 六项答辩级视频演示 | ❌ 待 S7 |
| §2.2 验收口径 LLM clarify | ✅ run `05-58-01` |
| H15/H16 验收追踪 | ✅ 24/28 AC verified；2 partial / 2 manual_pending |
| 公开 AI 系统主仓（内含 `sandbox-repo/`） | ❌ |
| 团队姓名 / Demo / 视频 URL | ❌ |
| 真实 draft PR（H17，可选） | ❌ 暂缓 |

**不能声称「课题完成」**，直到答辩视频、公开 AI 主仓、团队信息与对外链接补齐。

---

## 距离比赛目标仍缺什么（按优先级）

### P0 — 不补无法声称「课题完成」（§8.2 / 验收 F-011）

| 缺口 | PDF 对应 | 当前 | 行动 |
|------|----------|------|------|
| 演示视频（3–8 分钟） | §8.2 | ❌ | 按 [`video-recording-guide.md`](./bytedance-implementation/docs/reports/submission/video-recording-guide.md) 覆盖 §2.1 + §2.2 六项 |
| 公开源代码仓库 | §8.2 | ❌ | 公开 `bytedance-implementation/` 作为 AI 系统主仓，仓内包含 `sandbox-repo/`（见 [`public-repo-guide.md`](./bytedance-implementation/docs/reports/submission/public-repo-guide.md)） |
| 团队姓名 / Demo / 视频 URL | §8.2 | ❌ | 填写 [`team-info.md`](./bytedance-implementation/docs/reports/submission/team-info.md) |
| §2.2 六项统一答辩演示 | §2.2 + §6.2 | 🟡 | S7 录屏是六项从「有证据」到「可评判」的最后一环 |

### P1 — 不阻塞提交，影响答辩叙事与 §6 评分

| 缺口 | 说明 |
|------|------|
| 跨栈「自动驱动」未达 PDF 理想态 | §2.2 #3 要求后端字段变更自动驱动前端类型/Mock/调用点；当前为 Skill 手工跨路径 + `crossStackSync` 检查器 |
| Plan/Coding 以模板/Skill 驱动为主 | clarify 有真实 LLM；plan/code 仍以确定性模板和 Skill 写入为主，技术深度上限有限 |
| defense-prep 与视频未对齐 | ≤3 条亮点已有草稿，须与最终录屏脚本一致 |

### P2 — 可选 / 已决策跳过

| 项 | 状态 |
|----|------|
| H17 真实 GitHub draft PR | 暂缓（F-006-02 partial） |
| P1-5 补豆包 clarify | **团队决策跳过**（`mimo-v2.5` 模糊 run 已满足 #6） |
| sandbox-repo 8 项 npm audit | waiver B 已文档化（X1 已闭合） |

---

## 建议下一步（最小闭合路径，约 1–2 天人工）

1. **S7 答辩录屏（0.5–1 天）**
   - §2.1：rules 模式 L1 阅读量（`02-16-15-215Z`）— 输入 → diff → verification → PR 草稿
   - §2.2 #2：plan 后 `resume-from-stage` 只重跑 edit→verify→pr
   - §2.2 #1/#3：L2 跨栈（`05-52-12`）+ 第 4 Skill（Popular Tags，`06-24-47`）
   - §2.2 #4/#6：**不可降级** — 展示模糊 LLM clarify（`05-58-01`）+ 跨 run AI Usage 面板
   - 演示时 `AI_MODE=rules` 更稳；LLM 举证用已归档 run，不必现场重跑

2. **S8 公开 AI 系统主仓（~0.5 天）**：推送实现仓；确保仓内包含 `sandbox-repo/` Conduit 代码目录。

3. **S6 填链接（~0.5 天）**：`team-info.md` 写入团队名、成员、分工、Demo URL、视频链接。

4. **6.10 前 S9 复查**：发布日再跑 [`security-check-report.md`](./bytedance-implementation/docs/reports/submission/security-check-report.md)，确认无 key 入库。

5. **H17（可选）**：如需要演示远端 PR 能力，再配置 `GITHUB_TOKEN`、fork/分支权限并创建真实 draft PR，F-006-02 从 partial → verified。

**本地演示**：`cd bytedance-implementation && npm run dev`，打开 `http://localhost:5173`（勿与占 5173 端口的其它应用冲突）。

更细执行清单见 [`docs/06-plan.md#冲刺关键路径`](./docs/06-plan.md#冲刺关键路径进度-ssot) 与实现仓 [`defense-prep.md`](./bytedance-implementation/docs/reports/submission/defense-prep.md)。

---

## 本次更新摘要

- **对照源题 PDF 缺口复核**：新增「对照 PDF 完成度」与「距离比赛目标仍缺什么（P0/P1/P2）」；§2.2 表增加「代码 / 答辩/视频」分列。
- 明确课题完成阻塞项为 §8.2 对外交付四件套，非核心代码能力；验收 **24/28 AC verified**，AC-F011 三项仍待外部/人工闭合，AC-F006-02 真实远端 draft PR 为可选 partial。
- 明确公开提交目标是 AI 系统主仓（内含 `sandbox-repo/`），不是向上游 Conduit 直接提交；剩余 H17 draft PR 作为可选项保留在提交材料口径中；P1-5 豆包已决策跳过。
- 复核 `npm run verify`（108 项）、run 归档、第 4 Skill run `06-24-47`；补 Web controller、RunResult 面板、API run routes、runArchive reader/normalizer、runEvidenceGuards、deliveryPipeline 控制流与 aiCallRecords 拆分及行为测试。

---

*事实源索引见 [`README.md`](./README.md) 与 [`仓库导航.md`](./仓库导航.md)；详细进度见 [`docs/10-progress.md`](./docs/10-progress.md)。*
