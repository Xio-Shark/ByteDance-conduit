# 当前进度汇报：Conduit 超级个体端到端交付系统

更新时间：2026-05-21（与 `docs/10-progress.md` 同步）

## 总体结论

当前项目处于 **「§2.2 代码能力大部分已落地（rules 可演示 #1/#2/#3/#5；#4/#6 已有模糊 LLM 验收 run），§8.2 提交与答辩材料仍待闭合」** 的状态。

- **文档仓库** `/Users/xioshark/Desktop/bytedance`：只承担 `v1.0.0` 设计基线（PRD、架构、验收、追踪）；不含业务源码与运行证据。
- **实现仓库** `/Users/xioshark/Desktop/bytedance/bytedance-implementation`：三层应用、编排、Agent、Skill（3 个）、sandbox、断点重放、run 证据与提交材料草稿均已落地；代码级 P0 在 `AI_MODE=rules` 下闭环。
- **执行清单**：[`docs/06-plan.md#冲刺关键路径`](./docs/06-plan.md#冲刺关键路径进度-ssot)（已合并原根目录 CSV，根目录不再保留临时台账）。

规则模式已证明代码级 P0 胶水链路（澄清 → 历史召回 → 方案 → Conduit 前端 diff → lint/单测 → PR 草稿）。**H1–H12 代码冲刺**（断点重放、3 Skill、L2 跨栈、plan 引用召回）已落地；**课题完成仍缺**：§2.2 六项答辩级演示闭合、公开 AI 主仓 + `sandbox-repo/`、Demo、视频、团队信息、真实 draft PR 等（见 [`docs/01-understanding.md`](./docs/01-understanding.md) 完成定义分层）。

**LLM 口径**：`run-2026-05-21T05-58-01-181Z` 为 **模糊输入 + `clarifications[]` + 非零 tokens** 的验收口径 run（`mimo-v2.5`）；早期 `aiMode: doubao` 清晰 L1 原句 run **不计入 §2.2 #6**。中间曾有 `run:fuzzy-llm` 网关 502 失败归档，网关恢复后已成功重跑。

本次复核在实现仓库执行 `npm run verify`：**53 项 Node/API 测试、sandbox lint、Conduit Vitest（3 文件 / 12 测试）、Web 生产构建全部通过**。

---

## 仓库边界

| 仓库 | 职责 | 状态 |
|------|------|------|
| `/Users/xioshark/Desktop/bytedance` | `v1.0.0` 设计基线、PRD、架构、验收 YAML、本汇报 | 仅维护文档；`AGENTS.md` 限定可改范围 |
| `…/bytedance-implementation` | Web、API、Orchestrator、Agents、Skills、sandbox、证据、提交草稿 | P0 + §2.2 代码已落地；`npm run verify` 通过 |
| `…/bytedance-implementation/sandbox-repo` | Conduit 真实代码改动目标 | 阅读量 / 草稿 / 详情字数等多 Skill 改动 + `jsdom` 测试依赖 |

本文件是根目录**进度快照**，不替代唯一事实源。规格与验收以 `docs/01`–`12` 为准；**冲刺任务**以 [`docs/06-plan.md#冲刺关键路径`](./docs/06-plan.md#冲刺关键路径进度-ssot) 为准；运行证据以 `bytedance-implementation/docs/reports/**` 为准。

---

## 里程碑进度（对照 `docs/06-plan.md`）

| 里程碑 | 计划目标 | 当前状态 | 主要证据 |
|--------|----------|----------|----------|
| **M1** 课题理解与范围冻结 | Understanding / PRD / Spec / Design / Dev / Plan 对齐源题 | ✅ 已完成 | `docs/01`–`12` |
| **M2** 工程骨架与 Conduit 接入 | 三端可启动；sandbox 可读写 | ✅ 已完成 | `apps/web`、`apps/api`、`services/orchestrator`、`services/sandbox`、`.env.example` |
| **M3** P0 L1 链路跑通 | PM 输入 → 真实 diff | ✅ 已完成 | `run-2026-05-21T02-16-15-215Z` 等；`diff.patch` 含 Conduit 真实路径 |
| **M4** 验证与 PR 草稿闭环 | lint / 单测 + PR 描述 | ✅ 已完成 | `verification.json` 通过；`pr-draft.md` 已生成；GitHub draft PR provider 已实现（无远端 token 时仅契约测试） |
| **M5** §2.2 编排与体验 | 断点重放、LLM 澄清、监控面板、历史入 plan | 🟡 部分完成 | **resume-from-stage**、checkpoints、plan `history_references`、AI Usage 面板已闭合；H13 面板 non-zero badge 待与 H4 run 一并录制演示 |
| **M6** §2.2 能力与第二 Skill | 第 2/3 Skill、L2 跨栈 run | ✅ 已完成 | 3 Skill；L2 `run-2026-05-21T05-52-12-490Z`；第 3 Skill `run-2026-05-21T05-52-18-277Z` |
| **M7** 提交与答辩 | §8.2 材料、视频覆盖六项 | 🟡 草稿已生成 | `docs/reports/submission/*` 非空；人工链接、视频、公开双仓、真实 draft PR 待补 |

**官方时间节点**（来自 `docs/06-plan.md`）：实战期 2026-05-20 至 2026-06-10；成果提交截止 2026-06-10；答辩 2026-06-11 至 2026-06-19。距提交截止约 **20 天**。

---

## §2.2 评判亮点六项（对照 `docs/06-plan.md`）

| # | 亮点 | 状态 | 说明 | 降级路径 |
|---|------|------|------|---------|
| 1 | 抽象到位 | 🟡 rules 可演示 | 3 Skill（`article-list-display-field`、`article-draft-indicator`、`article-detail-word-count`）；第 3 个仅新增 Skill 文件 | 答辩前录制只加文件不改主干 |
| 2 | 断点重放 | 🟡 rules 可演示 | `POST .../resume-from-stage` + Web「Resume from edit (plan kept)」；`checkpoints.json` | 录制 plan 后只重跑 edit→verify→pr |
| 3 | 跨栈一致性 | 🟡 rules 可演示 | L2 `run-2026-05-21T05-52-12-490Z`；`impact_matrix.cross_stack: true` | — |
| 4 | 可观测性 | 🟡 验收 run 已有 | `run-2026-05-21T05-58-01-181Z` 非零 tokens/延迟/成本；面板 non-zero badge 已就绪 | **不可降级**；答辩须录面板 + ai-calls |
| 5 | 业务上下文反哺 | 🟡 rules 可演示 | `plan.history_references`；`run-2026-05-21T05-51-56-519Z`；召回状态 `degraded`（跳过坏归档） | — |
| 6 | 澄清深度 | 🟡 验收 run 已有 | 模糊输入 run `05-58-01` 含 `clarifications[]`；`ai-calls.jsonl` 非 rules | **不可降级**；答辩须展示追问证据 |

闭合依赖链、冲刺顺序见 [`docs/06-plan.md#冲刺关键路径`](./docs/06-plan.md#冲刺关键路径进度-ssot) 与 [`docs/03-spec.md`](./docs/03-spec.md)。

---

## 文档基线状态

| 文档 | 结论 |
|------|------|
| `README.md` | 明确文档仓只保存设计基线；区分代码级 P0 vs §2.2 + §8.2 课题完成 |
| `docs/01-understanding.md` | 题目目标、§2.2 六项必达、完成定义分层 |
| `docs/02-prd.md` | P0 覆盖澄清、Skill/Agent 分层、真实写入、验证、PR 草稿、提交材料、AI 留痕 |
| `docs/03-spec.md` | 可执行规格；规则模式 P0 闭环及 §2.2 实现层收口规则 |
| `docs/04-design.md` | 交付控制台：阶段、证据、diff、验证、PR 草稿、提交状态 |
| `docs/05-dev.md` | 实现仓目录、API、证据路径、安全配置 |
| `docs/06-plan.md` | M1–M4/M6 代码完成；M5/M7 / §2.2 答辩演示待闭合；**冲刺关键路径 H1–S10** |
| `docs/10-progress.md` | 实现进度事实源；与本汇报同步 |
| `docs/07`–`09` | 架构总览与 ADR |
| `docs/11-acceptance.yaml` / `docs/12-traceability.yaml` | P0/P1 验收与需求追踪（H15/H16 已闭合；27/30 AC verified） |

设计基线已合并为单一目录 [`docs/`](./docs/)，按流程编号 `01`–`12` 排布。

---

## 最新 Run 证据

### 代码级 P0（rules 主线）

| 项 | 当前值 |
|----|--------|
| **Run ID** | `run-2026-05-21T02-16-15-215Z` |
| **状态 / 阶段** | `passed` / `ready_for_pr` |
| **AI 模式** | `rules` |
| **Skill** | `article-list-display-field` |
| **证据目录** | `bytedance-implementation/docs/reports/runs/run-2026-05-21T02-16-15-215Z` |

**公开 L1 需求**：「给文章列表加阅读量展示，前端假数据即可，不改后端。」澄清结果排除后端 schema 与数据库迁移。

### §2.2 验收口径 LLM（模糊输入 + 追问）

| 项 | 当前值 |
|----|--------|
| **Run ID** | `run-2026-05-21T05-58-01-181Z` |
| **状态 / 阶段** | `passed` / `ready_for_pr` |
| **AI 模式** | `llm`（`mimo-v2.5`） |
| **模糊输入** | 「文章列表想好看一点，加点数据，别动太多代码。」 |
| **追问** | `requirement.md` 含 3 条 `clarifications[]` |
| **可观测** | tokens_in 248 / tokens_out 1818 / latency 21854ms / cost 0.004132 |
| **证据目录** | `…/runs/run-2026-05-21T05-58-01-181Z` |

### 其它 §2.2 关键 run

| Run ID | 用途 |
|--------|------|
| `run-2026-05-21T05-51-56-519Z` | 第二条相似需求；召回写入 `plan.history_references` |
| `run-2026-05-21T05-52-12-490Z` | L2 跨栈（frontend + backend）；`article-draft-indicator` |
| `run-2026-05-21T05-52-18-277Z` | 第 3 Skill：`article-detail-word-count` |

**累计 run 归档**：`docs/reports/runs/` 下共 **35** 个 run 目录（含成功与失败证据；2026-05-20 17:09 起至 2026-05-21 05:58）。

**历史召回**：最新成功 run 多为 `degraded`（匹配相似 run + `skipped` 坏归档），不把部分命中伪装成全量 ready。H1 已删除 3 条孤儿归档；仍有零星失败 run 缺 `requirement.md`/`plan.md`。

---

## 验证与工程质量

### 实现仓库完整验证（本次复核 `npm run verify`）

| 环节 | 结果 |
|------|------|
| Node/API 测试 | 通过（**53** 项；含 resume-from-stage、checkpoints、3 Skill、L2 跨栈/planning、history_references、LLM clarify 等） |
| `lint:sandbox` | 通过 |
| Conduit Vitest | 通过（3 文件 / 12 测试） |
| `apps/web` 生产构建 | 通过 |

实现仓根目录 `npm audit --audit-level=high`：**0 漏洞**。Conduit `sandbox-repo` 依赖树仍有 **8 项**（5 high、3 moderate，含 axios、lodash、vite 等上游），未执行 `npm audit fix`（X1 待提交前决策）。

---

## 已完成能力（实现仓）

- **三层 + 编排**：`apps/web`、`apps/api`、`services/orchestrator`，及 `agents`、`skills`、`sandbox`。
- **P0 主链路**：PM 输入 → 需求卡片 → 方案 / Skill 定位 → Conduit 写入 → 验证 → PR 草稿。
- **3 个 Skill**：`article-list-display-field`（L1）、`article-draft-indicator`（L2 跨栈）、`article-detail-word-count`（第 3 模式只加文件）。
- **断点重放**：`deliveryPipeline` 事件溯源 + `checkpoints.json`；`POST /api/runs/:id/resume-from-stage`；Web「Resume from edit (plan kept)」。
- **LLM 澄清**：`AI_MODE=llm` + `clarifyWithLlm`；模糊输入验收 run 已归档（见上表）。
- **历史召回入 plan**：`plan.history_references`；`history-recall` 支持 `degraded` / `skipped`。
- **严格失败语义**：缺字段、缺锚点、缺证据、无 Skill 匹配、不支持 `AI_MODE`、GitHub 非完整响应等直接失败或标 degraded。
- **Web/API**：run 查询、事件、diff、验证、PR 草稿、retry、confirm、resume-from-stage、submission 状态、AI Usage 面板（non-zero badge）。
- **归档与冷启动**：API 重启后可从 `docs/reports/runs/<id>` 恢复；confirm / PR 可写 metadata。
- **GitHub PR**：`POST /api/runs/:id/pr` + `external/git-provider`（H17：本地无 `GITHUB_TOKEN`）。
- **提交材料草稿**：checklist、demo-script、architecture、ai-usage、engineering-notes 均已生成且非空。

---

## 当前真实代码改动（sandbox-repo）

| 范围 | 文件 / 说明 |
|------|-------------|
| 文章列表阅读量 | `ArticlesPreview.jsx`、`styles.css` |
| L2 草稿状态 | `Article.js`、`articles.js`、列表卡片 |
| 详情字数 | `Article.jsx`、`styles.css` |
| 测试 | `package.json` / `package-lock.json` 增加 `jsdom` devDependency |

具体 diff 以各 run 的 `diff.patch` 为准；L2 run 含前后端路径。

---

## 提交材料状态（§8.2）

| 材料 | 路径 | 状态 |
|------|------|------|
| 提交清单 | `docs/reports/submission/checklist.md` | ✅ 已生成；团队/分工/Demo/视频/仓库链接 **待人工填写** |
| 演示脚本 | `demo-script.md` | ✅ 已生成 |
| 架构说明 | `architecture.md` | ✅ 已生成 |
| AI 使用说明 | `ai-usage.md` | ✅ 已生成；模型清单表 / 合规段 **待 H4 后补全** |
| 工程难点 | `engineering-notes.md` | ✅ 已生成 |

---

## 完成判定（对照 `README.md`）

### 代码级 P0

| 检查项 | 状态 |
|--------|------|
| 三端可启动 | ✅ |
| sandbox 基于 Conduit 真实路径 | ✅ |
| 至少一条 L1 需求 → PR 草稿 | ✅（规则模式） |
| `diff.patch` / `verification.json` / `pr-draft.md` / `ai-calls.jsonl` | ✅ |
| 无真实 API key 泄露 | ✅ |

### 课题完成（§2.1 + §2.2 + §8.2）

| 检查项 | 状态 |
|--------|------|
| §2.2 六项答辩级演示全部闭合 | ❌（代码与验收 run 已有，待视频逐项展示） |
| §2.2 验收口径 LLM clarify（模糊输入 + 追问） | 🟡 run `05-58-01` 已归档；待写入 `ai-usage.md` 模型表与 S5 对话材料 |
| 公开 AI 主仓 + `sandbox-repo/` | ❌ |
| 在线 Demo / 演示视频 | ❌ |
| 团队名称与分工 | ❌ |
| 真实 draft PR URL（H17） | ❌ |

**不能声称「课题完成」或「最终提交已完成」**，直到 §2.2 六项答辩演示、§8.2 材料、公开双仓、Demo、视频与团队信息补齐。

---

## 当前缺口与建议下一步

按 [`docs/06-plan.md#冲刺关键路径`](./docs/06-plan.md#冲刺关键路径进度-ssot)：

1. **答辩演示**：对照六项录制 3–8 分钟视频（S7）；Web 演示 resume-from-stage、L2 跨栈、第三 Skill、AI Usage 非零 metrics。
2. **提交线 S1–S10**：补 `prompt-changelog.md`、完善 `ai-usage.md` 模型清单与合规段、导出 H4 多轮澄清材料（S5）、公开双仓（S8）、6.10 前安全检查（S9）。
3. **提交线**：S1/S5–S10（prompt-changelog、澄清对话导出、公开双仓、视频）；H17 真实 draft PR 已暂缓。
4. **风险 X1**：决策 `sandbox-repo` 8 项上游 audit 是否 fix。

---

## 本次复核操作摘要

- 按 `README.md` 阅读顺序核对 `AGENTS.md`、`docs/01`–`12`（含 §2.2 六项与冲刺表 H1–S10）。
- 读取实现仓最新 run 证据（含模糊 LLM `05-58-01`、L2 `05-52-12`、第三 Skill `05-52-18`）及 `docs/10-progress.md`、`docs/06-plan.md`。
- 在实现仓执行 `npm run verify`（**53** 项测试全部通过）。
- 统计 `docs/reports/runs/` 共 **35** 个 run 目录；确认 submission 五份草稿均存在。

---

*事实源索引见 [`README.md`](./README.md)「唯一事实源」表。*
